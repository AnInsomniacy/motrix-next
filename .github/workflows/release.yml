name: Release

on:
  release:
    types: [published]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: macos-15
            args: --target aarch64-apple-darwin
            label: macos-aarch64
          - platform: macos-15-intel
            args: --target x86_64-apple-darwin
            label: macos-x86_64
          - platform: windows-2025-vs2026
            args: --target x86_64-pc-windows-msvc --bundles nsis
            label: windows-x86_64
          - platform: windows-2025-vs2026
            args: --target aarch64-pc-windows-msvc --bundles nsis
            label: windows-aarch64
          - platform: ubuntu-22.04
            args: --target x86_64-unknown-linux-gnu
            label: linux-x86_64
          - platform: ubuntu-22.04-arm
            args: --target aarch64-unknown-linux-gnu
            label: linux-aarch64

    runs-on: ${{ matrix.platform }}

    steps:
      - uses: actions/checkout@v6

      - uses: pnpm/action-setup@v6

      - uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: pnpm

      - uses: dtolnay/rust-toolchain@stable

      # Cross-compilation: add the secondary Rust target when building for
      # a different architecture on the same OS runner.
      # - macOS x86_64 uses a native Intel runner (macos-13), no cross-compile needed.
      # - Windows ARM64 cross-compiles on x64 runner (NSIS is arch-agnostic).
      - name: Add cross-compilation target
        if: contains(matrix.args, 'aarch64-pc-windows-msvc')
        shell: bash
        run: |
          TARGET=$(echo '${{ matrix.args }}' | sed 's/.*--target \([^ ]*\).*/\1/')
          rustup target add "$TARGET"

      - uses: swatinem/rust-cache@v2

      # Suppress needrestart daemon (auto-restarts services on package
      # install, can kill the runner agent and cause "operation canceled").
      - name: Install Linux dependencies
        if: runner.os == 'Linux'
        run: |
          export NEEDRESTART_SUSPEND=1
          export DEBIAN_FRONTEND=noninteractive
          sudo apt-get update
          sudo -E apt-get install -y \
            libwebkit2gtk-4.1-dev libayatana-appindicator3-dev \
            librsvg2-dev patchelf xdg-utils

      - run: pnpm install

      - name: Configure Rayburst update signing
        shell: bash
        env:
          RAYBURST_UPDATER_PUBLIC_KEY: ${{ vars.RAYBURST_UPDATER_PUBLIC_KEY }}
        run: node scripts/configure-release.mjs

      - uses: tauri-apps/tauri-action@v0
        id: tauri
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.RAYBURST_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.RAYBURST_SIGNING_PRIVATE_KEY_PASSWORD }}
        with:
          tagName: ${{ github.event.release.tag_name || '' }}
          releaseId: ${{ github.event_name == 'release' && github.event.release.id || '' }}
          includeUpdaterJson: false
          args: ${{ matrix.args }} --config src-tauri/tauri.release.local.json

      - name: Upload build artifacts (manual trigger only)
        if: github.event_name == 'workflow_dispatch'
        uses: actions/upload-artifact@v7
        with:
          name: rayburst-${{ matrix.label }}
          path: "${{ join(fromJSON(steps.tauri.outputs.artifactPaths), '\n') }}"
          retention-days: 7

  merge-updater-json:
    needs: build
    runs-on: ubuntu-latest
    if: github.event_name == 'release'

    steps:
      - uses: actions/checkout@v6

      - name: Generate and upload channel-specific updater JSON
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          ./.github/scripts/refresh-updater-json.sh "${{ github.event.release.tag_name }}"

  update-homebrew:
    needs: build
    runs-on: ubuntu-latest
    if: github.event_name == 'release' && !github.event.release.prerelease && vars.RAYBURST_HOMEBREW_ENABLED == 'true'

    steps:
      - name: Download macOS artifacts and compute checksums
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          TAG="${{ github.event.release.tag_name }}"
          VERSION="${TAG#v}"

          gh release download "$TAG" \
            -R "${{ github.repository }}" \
            -p "Rayburst_aarch64.app.tar.gz" \
            -p "Rayburst_x64.app.tar.gz"

          ARM_SHA=$(shasum -a 256 Rayburst_aarch64.app.tar.gz | cut -d ' ' -f1)
          INTEL_SHA=$(shasum -a 256 Rayburst_x64.app.tar.gz | cut -d ' ' -f1)

          echo "VERSION=$VERSION" >> "$GITHUB_ENV"
          echo "ARM_SHA=$ARM_SHA" >> "$GITHUB_ENV"
          echo "INTEL_SHA=$INTEL_SHA" >> "$GITHUB_ENV"

      - name: Update Homebrew Cask
        env:
          HOMEBREW_TAP_TOKEN: ${{ secrets.HOMEBREW_TAP_TOKEN }}
        run: |
          git clone "https://x-access-token:${HOMEBREW_TAP_TOKEN}@github.com/AnInsomniacy/homebrew-rayburst.git"
          cd homebrew-rayburst

          # Generate the entire cask file from a template instead of using
          # fragile sed replacements. Shell expands $VERSION, $ARM_SHA, and
          # $INTEL_SHA; Ruby interpolations (#{version}, #{arch}) pass through
          # literally because #{ is not shell syntax.
          cat > Casks/rayburst.rb << CASK_EOF
          cask "rayburst" do
            arch arm: "aarch64", intel: "x64"

            version "$VERSION"
            sha256 arm:   "$ARM_SHA",
                   intel: "$INTEL_SHA"

            url "https://github.com/AnInsomniacy/rayburst/releases/download/v#{version}/Rayburst_#{arch}.app.tar.gz"
            name "Rayburst"
            desc "Full-featured download manager built with Tauri"
            homepage "https://github.com/AnInsomniacy/rayburst"

            livecheck do
              url :url
              strategy :github_latest
            end

            auto_updates true

            app "Rayburst.app"

            zap trash: [
              "~/Library/Application Support/dev.aninsomniacy.rayburst",
              "~/Library/Caches/dev.aninsomniacy.rayburst",
              "~/Library/Logs/dev.aninsomniacy.rayburst",
              "~/Library/WebKit/dev.aninsomniacy.rayburst",
            ]
          end
          CASK_EOF

          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add Casks/rayburst.rb
          git diff --cached --quiet && echo "No changes to commit" && exit 0
          git commit -m "rayburst $VERSION"
          git push
